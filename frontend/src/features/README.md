# Feature modules

Folders are reserved for Backend V2–native modules.

**Do not implement operational CRUD until Phase 1 is approved.**

Each module will own: `api.ts`, hooks (React Query), pages, dialogs, tables, tests.
